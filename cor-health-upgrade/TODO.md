# TODO

## Goal: Make the whole website use a sans-serif font

- [ ] Update `app/globals.css` so `body` uses a generic sans-serif stack.
- [ ] Update `tailwind.config.ts` to ensure `font-poppins` and `font-inter` utilities fall back to the generic sans-serif stack (so `font-poppins` classes no longer force Poppins).
- [ ] (Optional sanity check) Ensure no other font utilities/classes override the base font.

